{
  "name": "@neurodevs/commit-sense",
  "version": "0.1.0",
  "description": "Sense what happens downstream before you commit.",
  "type": "module",
  "keywords": [
    "nodejs",
    "typescript",
    "tdd",
    "pre-ci",
    "continuous",
    "integration",
    "git",
    "crossrepo",
    "commits",
    "build",
    "test",
    "inspect",
    "refactor"
  ],
  "license": "MIT",
  "author": "Eric Yates <hello@ericthecurious.com>",
  "homepage": "https://github.com/neurodevs/commit-sense",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neurodevs/commit-sense.git"
  },
  "bugs": {
    "url": "https://github.com/neurodevs/commit-sense/issues"
  },
  "main": "build/extensions.js",
  "publisher": "neurodevs",
  "displayName": "CommitSense",
  "engines": {
    "vscode": "^1.85.0"
  },
  "activationEvents": [
    "onStartupFinished",
    "workspaceContains:.git"
  ],
  "contributes": {
    "commands": [
      {
        "command": "commitSense.start",
        "title": "Start CommitSense"
      }
    ]
  },
  "scripts": {
    "build.ci": "yarn run build.tsc && yarn run build.resolve-paths && yarn run lint",
    "build.dev": "yarn run build.tsc --sourceMap ; yarn run resolve-paths.lint",
    "build.copy-files": "mkdir -p build && rsync -avzq --exclude='*.ts' ./src/ ./build/",
    "build.resolve-paths": "resolve-path-aliases --target build --patterns '**/*.js,**/*.d.ts'",
    "build.tsc": "yarn run build.copy-files && tsc",
    "clean": "yarn run clean.build",
    "clean.all": "yarn run clean.dependencies && yarn run clean.build",
    "clean.build": "rm -rf build/",
    "clean.dependencies": "rm -rf node_modules/ package-lock.json yarn.lock",
    "fix.lint": "eslint --fix --cache '**/*.ts'",
    "lint": "eslint --cache '**/*.ts'",
    "lint.tsc": "tsc -p . --noEmit",
    "post.watch.build": "yarn run build.copy-files && yarn run build.resolve-paths",
    "rebuild": "yarn run clean.all && yarn install && yarn run build.dev",
    "update.dependencies": "yarn run clean.dependencies && yarn",
    "resolve-paths.lint": "yarn run build.resolve-paths ; yarn run lint",
    "test": "jest",
    "watch.build.dev": "tsc-watch --sourceMap --onCompilationComplete 'yarn run post.watch.build'",
    "watch.rebuild": "yarn run clean.all && yarn install && yarn run watch.build.dev",
    "watch.tsc": "tsc -w"
  },
  "dependencies": {
    "@neurodevs/meta-node": "^0.14.26"
  },
  "devDependencies": {
    "@neurodevs/fake-node-core": "^0.7.2",
    "@neurodevs/generate-id": "^1.0.9",
    "@neurodevs/node-tdd": "^0.2.2",
    "@types/node": "^24.6.2",
    "chokidar-cli": "^3.0.0",
    "eslint": "^9.37.0",
    "eslint-config-spruce": "^11.2.26",
    "jest": "^30.2.0",
    "jest-circus": "^30.2.0",
    "module-alias": "^2.2.3",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "tsc-watch": "^7.2.0",
    "typescript": "^5.9.3"
  },
  "jest": {
    "testRunner": "jest-circus/runner",
    "testEnvironment": "node",
    "testTimeout": 120000,
    "maxWorkers": 4,
    "testPathIgnorePatterns": [
      "<rootDir>/tmp/",
      "<rootDir>/src/",
      "<rootDir>/node_modules/",
      "<rootDir>/build/__tests__/testDirsAndFiles/"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.js?(x)"
    ],
    "moduleNameMapper": {
      "^#spruce/(.*)$": "<rootDir>/build/.spruce/$1",
      "^vscode$": "<rootDir>/build/testDoubles/vscode/fakeVscode.js"
    }
  },
  "skill": {
    "namespace": "commit-sense"
  }
}
